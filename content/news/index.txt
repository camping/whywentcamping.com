---
title:       Camping, the News
dirty:       yes
filter:
  - erb
---


<% pages = @pages.find(:limit => :all,
                       :in_directory => 'news',
                       :sort_by => 'created_at').reverse %> 

<% pages.each do |page| %>
<% next if page == @page %>
  <%= render(page) %>
<% end %>

